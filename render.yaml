services:
  - type: web
    name: kansei-link-mcp
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "3000"
      - key: KANSEI_HOST
        value: "0.0.0.0"
