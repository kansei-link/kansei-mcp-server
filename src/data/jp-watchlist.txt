# Japanese SaaS MCP watchlist — hand-curated.
#
# Lines in this file are picked up by the crawler source
# src/crawler/sources/jp-watchlist.ts on every daily run and fed
# through the same dedupe → enrich → classify → score pipeline as
# GitHub-topics and awesome-list hits.
#
# Why this exists: the global MCP Registry and awesome-mcp-servers
# lists systematically under-represent Japanese SaaS. JP vendors tend
# to announce on X / PR TIMES / Zenn — not GitHub topics.
# This file is where Michie (or any agent acting on her behalf) drops
# candidates spotted in the wild so the next crawl ingests them.
#
# Format: one `owner/repo` per line. Lines starting with `#` are
# comments. Optional inline comment after `  #` is ignored at parse
# time but preserved for humans tracking provenance.
#
# Example:
#   DifyJapan/dify-mcp-server  # spotted via @DifyJapan on X 2026-04-21

DifyJapan/dify-mcp-server  # spotted via @DifyJapan on X 2026-04-21
